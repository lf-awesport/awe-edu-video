# Rollout T1–T10 / Piano di rollout T1–T10

**Candidate normative bilingual annex / Allegato normativo candidato bilingue condiviso.** Applicable requirement IDs: **ROLLOUT-001–ROLLOUT-003**. It becomes effective only with the approved parent specification. Before T1, gate **G0 Contract Bootstrap** MUST provide compilable versioned schemas for the T1 records, canonical JSON/hash rules, valid/invalid fixtures, an offline contract-test harness, and an approved Remotion version/licence/use/cost decision. T1–T3 then proceed primarily serially; later work MAY advance in parallel only against versioned stable contract artifacts. / Prima di T1, G0 DEVE produrre schema/harness compilabili e decisione Remotion; poi T1–T3 principalmente seriali e parallelismo solo su contratti versionati.

| Tracer | Hypothesis + observable path / Ipotesi e percorso osservabile | In scope / Out of scope | Principal failure | Artifacts/evidence | Dependencies | Exit gate | Applicable requirement IDs |
|---|---|---|---|---|---|---|---|
| T1 | Canonical Scene 3 compiles/renders deterministically: `validate→build --scene scene-03→render→verify`. / S3 deterministica. | Model, IDs, real local asset, RenderPlan, renderer sandbox / provider, audio, full UI | model leaks renderer/provider or nondeterministic hash | fixture, plan/hash, render, full decode, sandbox and licensing evidence | G0 | canonical plan hashes equal; technical media checks pass; bad IDs/assets/network/write fail | ROLLOUT-001, ROLLOUT-003 |
| T2 | Desired state reconciles to replayable DAG/cache: `build→status/events→invalidate→state reconstruct`. | events/snapshot/hash cache/selective invalidation/lease recovery / paid submit | stale cache or irreconstructable/crash-corrupt state | event log, prefix/snapshot hash equality, E03, crash/integrity/fencing matrix | T1 model/hash contract | replay equality; gap/divergence/truncated-tail/stale-writer cases fail or recover as specified | ROLLOUT-001, ROLLOUT-003 |
| T3 | Exact Higgsfield request is discoverable, freshly bound/reserved and materialized once: `provider doctor→preflight→apply`. | adapter lifecycle, price/unit/batch Quote binding, atomic Budget, materialization, offline denial / creative acceptance, uncertain-submit recovery | schema/price drift, duplicate charge, bad media | redacted CLI/version/schema snapshot, fresh binding, plan, ledger, Candidate/Blob, fake/replay and authorized minimal live smoke | T1–T2 contracts stable; outbound-data/terms/Budget approval for live | fake/replay pass; stale binding/offline/bad media fail; live evidence captures receipt/output/actual charge without secrets | ROLLOUT-001, ROLLOUT-003 |
| T4 | Crash after submit reconciles without blind resubmit: `apply→crash fixture→reconcile`. | write-ahead intent/job lookup/charge states / generic scheduler | duplicate submit or unknown charge released | SubmissionIntent, Reservation, snapshots/events, provider replay | T3 | every crash boundary reaches known terminal or explicit reconciling | ROLLOUT-001, ROLLOUT-003 |
| T5 | S1–S3 audio and transition follow selected voice and exact boundaries: `build --scene→candidate select→render→verify`. | scripts/resolved candidate counts/captions/mix/stems/transition invalidation / all locales | retake bypasses timing change-control or consent expires | scripts, candidates, captions, mix, E03, AV/consent report | T1–T2; T3 only for S1 footage | AV, low-confidence alignment, granular invalidation, consent-expiry and out-of-tolerance proposal fixtures pass | ROLLOUT-001, ROLLOUT-003 |
| T6 | Claims and decisions fail closed but permit internal preview: `normalize→promote→review→explain --blocked`. | source refs/report/authority-bound Approval/Waiver/Review Queue / complete AWE assets | injected source changes policy or local profile spoofs legal role | normalization report, source snapshots, decision log, blocker matrix | T1 model; T2 state; may parallel T4/T5 after contracts | multimodal/metamorphic injection, conflict, authority-spoof and supersession fixtures pass | ROLLOUT-002, ROLLOUT-003 |
| T7 | Full 13-scene Master compiles without AWE special cases: `build --to preview`. | renderer UI, typed non-promotable placeholders, usage coverage / release assertion | missing scene/layer or generated UI authority | synthetic compilable AWE fixture, requested 85 s, resolved timing delta, preview, usage/coverage report | T1; T5 timing; T6 Claim gates; provider assets as needed | exactly 13 scenes; requested 85 s preserved; resolved duration explained; blockers machine-visible | ROLLOUT-002, ROLLOUT-003 |
| T8 | 9:16/localized variants are stable-ID overrides and fail on lost essentials: `build --variant→verify`. | layout strategy/local script/caption/full render / implicit frame reuse | crop loses logo/CTA or master mutation | variant diff/plan/render/findings | T1 model; T5 audio; T7 master; may develop adapters earlier | semantic invariants and negative crop fixture pass | ROLLOUT-002, ROLLOUT-003 |
| T9 | Quality/governance produces tamper-evident non-waivable release gate: `verify→release create→export`. | evaluators, rights, cost, approval, manifests, portable export / closing real AWE inputs | stale/missing evidence marked releasable | Quality Report, locks, usage/release manifests, export tests | T2; contracts from T5–T8; quality/governance work may parallel them | complete/stale/tampered matrices fail/pass correctly offline | ROLLOUT-002, ROLLOUT-003 |
| T10 | Real AWE inputs close all blockers and produce accepted deliverables: full journey. | actual sources/assets/grants/thresholds/Quotes/master+required variants / product expansion | evidence current at review but stale at release | private fixture inventory/hash with redacted public manifest, authority-bound decisions, fresh snapshots/Quotes, final reports/manifests/renders | exit gates T1–T9 plus explicit entry gate: fixture access, owners/reviewers, Claims/assets/grants, Budget/provider/output policies ready | freshness rechecked immediately; owner/brand/legal/language acceptance; `releasable` current | ROLLOUT-002, ROLLOUT-003 |

Each tracer MUST own an executable acceptance manifest with command, fixture/test IDs, environment/toolchain, expected exit/output, artifact/evidence path and approver when human; traceability MUST point to those IDs rather than prose labels. Normal CI MUST remain offline. Promotion to internal pilot or release candidate MUST also have a rehearsed reversal plan: stop new submits/releases, demote/quarantine artifacts, preserve evidence, reconcile in-flight provider jobs/charges, and restore a compatible code/schema version. Reversal MUST NOT promise undo of an irreversible provider charge. / Ogni tracer DEVE avere acceptance manifest eseguibile; promotion DEVE avere reversal provato senza promettere undo di costi provider irreversibili.
