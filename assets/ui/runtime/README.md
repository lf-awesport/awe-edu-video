# Runtime UI assets

Deterministic 1080 px-wide Lanczos downscales of the supplied 3× PNG mockups in `../mockups/`.
They preserve the native Figma design dimensions while avoiding repeated decoding of 32-megapixel source exports during Full HD rendering.
The original files remain unchanged and authoritative; these files are render derivatives only.
